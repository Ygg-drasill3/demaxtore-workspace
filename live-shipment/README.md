# live-shipment (trimmed)

Reference source for the DeMaxtore **Live Map** preview.

Integrated into `apps/frontend`:
- `features/shipment/components/LiveShipmentMap.tsx` — MapLibre preview
- Buyer dashboard Operations section
- Shipment workspace journey map
- Shipment portfolio map

Kept here for reference only (not a runnable app):
- `map-source/components/MapView.jsx` — original MapLibre vessel map
- `lib/` — vesselMeta / api / utils from the prototype
- `backend-ais/` — mock AIS provider + port data
- `reference-*.json|py|txt` — original package/server stubs

Do not reinstall `node_modules` here.
