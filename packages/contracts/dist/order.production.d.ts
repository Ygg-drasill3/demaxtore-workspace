/** Production progress must reach 100% before order advances past production. */
export declare const PRODUCTION_COMPLETE_PERCENT = 100;
export declare function isProductionCompletePercent(value: unknown): boolean;
