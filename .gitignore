# GitHub rejects files over 100MB. These trees contain such binaries.
.data/
.tools/
.deploy-last-good
apps/frontend/dist-backup-*/

# Nested git metadata (not source)
**/.git/

# Editor crash / OS junk only
.DS_Store
.ipynb_checkpoints/
